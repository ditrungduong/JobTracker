const db = require('./database'); // Reuse the database module

// Log a message to confirm the database initialization
console.log('Database initialization complete. The schema is already ensured.');
